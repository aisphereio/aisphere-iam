# Mainline CI policy

CI validates the checked-out commit, uses the same Kernel version for runtime and code generation, preserves verbose test logs as artifacts, and never commits or pushes source changes.
