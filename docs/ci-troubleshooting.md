# CI troubleshooting

When a Go test fails, download the `iam-go-test-log` artifact from the failed CI run and inspect the final failing package and assertion. Do not infer the failure from truncated Actions UI output.
